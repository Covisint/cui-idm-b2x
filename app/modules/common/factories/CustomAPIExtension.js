angular.module('common')
.factory('CustomAPIExtension', () => {

    const calls = [
        /**
            Any calls added here will be automatically added to the API.cui Object.
        **/
    ];

    return calls

})
